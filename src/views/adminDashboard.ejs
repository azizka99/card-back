<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>CardScan Admin Dashboard</title>

    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      rel="stylesheet"
    />
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <style>
      :root {
        --primary: #6262a6;
        --primary-dark: #4e4e85;
        --primary-light: #eef0f7;
        --text-dark: #1e293b;
        --text-gray: #64748b;
        --bg-body: #f1f5f9;
        --white: #ffffff;
        --shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1);
        --sidebar-width: 260px;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Inter", sans-serif;
      }

      body {
        background-color: var(--bg-body);
        display: flex;
        min-height: 100vh;
      }

      /* --- SIDEBAR --- */
      .sidebar {
        width: var(--sidebar-width);
        background-color: var(--primary);
        color: var(--white);
        display: flex;
        flex-direction: column;
        position: fixed;
        height: 100vh;
        left: 0;
        top: 0;
        padding: 24px;
        z-index: 50;
      }

      .logo {
        font-size: 22px;
        font-weight: 700;
        display: flex;
        align-items: center;
        gap: 12px;
        margin-bottom: 40px;
        letter-spacing: -0.5px;
      }

      .menu-label {
        font-size: 12px;
        text-transform: uppercase;
        color: rgba(255, 255, 255, 0.6);
        margin-bottom: 10px;
        font-weight: 600;
        letter-spacing: 0.5px;
      }

      .menu {
        list-style: none;
        display: flex;
        flex-direction: column;
        gap: 8px;
      }

      .menu li a {
        text-decoration: none;
        color: rgba(255, 255, 255, 0.8);
        padding: 12px 16px;
        border-radius: 8px;
        display: flex;
        align-items: center;
        gap: 12px;
        font-size: 14px;
        font-weight: 500;
        transition: all 0.2s;
      }

      .menu li a:hover,
      .menu li a.active {
        background-color: rgba(255, 255, 255, 0.15);
        color: var(--white);
      }

      .menu li a i {
        width: 20px;
        text-align: center;
      }

      /* --- MAIN CONTENT --- */
      .main-wrapper {
        margin-left: var(--sidebar-width);
        flex: 1;
        display: flex;
        flex-direction: column;
      }

      /* --- HEADER --- */
      header {
        background-color: var(--white);
        height: 70px;
        padding: 0 32px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        box-shadow: var(--shadow);
        position: sticky;
        top: 0;
        z-index: 40;
      }

      .search-bar {
        display: flex;
        align-items: center;
        background-color: var(--bg-body);
        padding: 10px 16px;
        border-radius: 8px;
        width: 350px;
        border: 1px solid transparent;
        transition: 0.2s;
      }

      .search-bar:focus-within {
        border-color: var(--primary);
        background-color: var(--white);
      }

      .search-bar i {
        color: var(--text-gray);
        margin-right: 10px;
      }

      .search-bar input {
        border: none;
        background: none;
        outline: none;
        width: 100%;
        font-size: 14px;
        color: var(--text-dark);
      }

      .header-actions {
        display: flex;
        align-items: center;
        gap: 20px;
      }

      .icon-wrapper {
        position: relative;
        cursor: pointer;
        width: 40px;
        height: 40px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 50%;
        transition: 0.2s;
      }

      .icon-wrapper:hover {
        background-color: var(--bg-body);
        color: var(--primary);
      }

      .badge {
        position: absolute;
        top: 8px;
        right: 8px;
        width: 8px;
        height: 8px;
        background-color: #ef4444;
        border-radius: 50%;
        border: 2px solid var(--white);
      }

      /* --- DASHBOARD CONTENT --- */
      .content {
        padding: 32px;
      }

      .page-title {
        font-size: 24px;
        font-weight: 700;
        color: var(--text-dark);
        margin-bottom: 24px;
      }

      /* Stat Cards */
      .stats-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 24px;
        margin-bottom: 32px;
      }

      .stat-card {
        background: var(--white);
        padding: 24px;
        border-radius: 12px;
        box-shadow: var(--shadow);
        display: flex;
        flex-direction: column;
        justify-content: space-between;
      }

      .stat-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 16px;
      }

      .stat-title {
        color: var(--text-gray);
        font-size: 13px;
        font-weight: 600;
        text-transform: uppercase;
      }

      .stat-icon {
        width: 40px;
        height: 40px;
        border-radius: 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 18px;
      }

      .bg-purple {
        background: rgba(98, 98, 166, 0.1);
        color: var(--primary);
      }
      .bg-green {
        background: #dcfce7;
        color: #16a34a;
      }
      .bg-orange {
        background: #ffedd5;
        color: #ea580c;
      }
      .bg-red {
        background: #fee2e2;
        color: #dc2626;
      }

      .stat-value {
        font-size: 28px;
        font-weight: 700;
        color: var(--text-dark);
      }

      .stat-diff {
        font-size: 12px;
        margin-top: 8px;
        font-weight: 500;
      }

      .text-up {
        color: #16a34a;
      }
      .text-down {
        color: #dc2626;
      }

      /* Analytics Section */
      .analytics-grid {
        display: grid;
        grid-template-columns: 2fr 1fr;
        gap: 24px;
      }

      .panel {
        background: var(--white);
        border-radius: 12px;
        box-shadow: var(--shadow);
        padding: 24px;
        display: flex;
        flex-direction: column;
      }

      .panel-title {
        font-size: 18px;
        font-weight: 600;
        color: var(--text-dark);
        margin-bottom: 20px;
      }

      /* --- CRITICAL FIX: Fixed Height Container for Chart --- */
      .chart-container {
        position: relative;
        height: 400px; /* Fixed height stops it from growing forever */
        width: 100%;
      }

      /* List Styling */
      .user-list {
        list-style: none;
        overflow-y: auto;
        max-height: 400px; /* Aligns with chart height */
      }

      .user-item {
        display: flex;
        align-items: center;
        padding: 12px 0;
        border-bottom: 1px solid #f1f5f9;
      }

      .user-item:last-child {
        border-bottom: none;
      }

      .avatar {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        background-color: var(--primary-light);
        color: var(--primary);
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 700;
        font-size: 14px;
        margin-right: 12px;
      }

      .user-info h4 {
        font-size: 14px;
        color: var(--text-dark);
        margin-bottom: 2px;
      }

      .user-info p {
        font-size: 12px;
        color: var(--text-gray);
      }

      .scan-count {
        margin-left: auto;
        font-weight: 600;
        color: var(--primary);
        font-size: 14px;
      }
      /* --- NOTIFICATION UPDATES --- */

      /* Updated Badge to hold numbers */
      .badge {
        position: absolute;
        top: -2px;
        right: -2px;
        min-width: 18px;
        height: 18px;
        padding: 0 4px;
        background-color: #ef4444;
        border-radius: 10px;
        border: 2px solid var(--white);
        color: white;
        font-size: 10px;
        font-weight: 700;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      /* Dropdown Container */
      .notification-dropdown {
        display: none; /* Hidden by default */
        position: absolute;
        top: 55px;
        right: -10px; /* Aligns with the right edge of icon wrapper */
        width: 300px;
        background: var(--white);
        border-radius: 12px;
        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1),
          0 4px 6px -2px rgba(0, 0, 0, 0.05);
        border: 1px solid #e2e8f0;
        z-index: 100;
        overflow: hidden;
        cursor: default; /* Reset cursor inside dropdown */
      }

      /* Show class for JS toggle */
      .notification-dropdown.active {
        display: block;
        animation: fadeIn 0.2s ease-out;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(-10px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .notif-header {
        padding: 16px;
        font-weight: 600;
        font-size: 14px;
        color: var(--text-dark);
        border-bottom: 1px solid #f1f5f9;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      .mark-read {
        font-size: 11px;
        color: var(--primary);
        cursor: pointer;
      }

      .notif-list {
        max-height: 300px;
        overflow-y: auto;
      }

      .notif-item {
        padding: 12px 16px;
        display: flex;
        gap: 12px;
        border-bottom: 1px solid #f1f5f9;
        transition: background 0.2s;
        cursor: pointer;
      }

      .notif-item:hover {
        background-color: #f8fafc;
      }

      .notif-item:last-child {
        border-bottom: none;
      }

      /* Notification Avatar */
      .notif-img {
        width: 36px;
        height: 36px;
        border-radius: 50%;
        object-fit: cover;
        border: 1px solid #e2e8f0;
      }

      .notif-content {
        flex: 1;
      }

      .notif-text {
        font-size: 13px;
        color: var(--text-gray);
        line-height: 1.4;
        margin-bottom: 4px;
      }

      .notif-text strong {
        color: var(--text-dark);
        font-weight: 600;
      }

      .notif-time {
        font-size: 11px;
        color: #94a3b8;
      }

      /* Responsive */
      @media (max-width: 1024px) {
        .stats-grid {
          grid-template-columns: repeat(2, 1fr);
        }
        .analytics-grid {
          grid-template-columns: 1fr; /* Stack chart and list on tablets */
        }
        .chart-container {
          height: 300px; /* Slightly smaller on tablets */
        }
      }

      @media (max-width: 768px) {
        .sidebar {
          display: none;
        }
        .main-wrapper {
          margin-left: 0;
        }
        .stats-grid {
          grid-template-columns: 1fr;
        }
        header {
          padding: 0 16px;
        }
        .search-bar {
          width: 200px;
        }
      }
    </style>
  </head>
  <body>
    <aside class="sidebar">
      <div class="logo">
        <i class="fas fa-layer-group"></i>
        <span>CardScan.io</span>
      </div>

      <div class="menu-label">Analytics</div>
      <ul class="menu">
        <li>
          <a href="#" class="active"
            ><i class="fas fa-chart-pie"></i> Dashboard</a
          >
        </li>
        <li>
          <a href="#"><i class="fas fa-qrcode"></i> Live Feed</a>
        </li>
        <li>
          <a href="#"><i class="fas fa-database"></i> Scan History</a>
        </li>
      </ul>

      <div class="menu-label" style="margin-top: 24px">Admin</div>
      <ul class="menu">
        <li>
          <a href="#"><i class="fas fa-users"></i> Users</a>
        </li>
        <li>
          <a href="#"><i class="fas fa-shield-alt"></i> Permissions</a>
        </li>
        <li>
          <a href="#"><i class="fas fa-cog"></i> Settings</a>
        </li>
      </ul>
    </aside>

    <div class="main-wrapper">
      <header>
        <div class="search-bar">
          <i class="fas fa-search"></i>
          <input type="text" placeholder="Search card ID or user..." />
        </div>

        <div class="header-actions">
          <div class="icon-wrapper" id="notificationBtn">
            <i class="far fa-bell" style="font-size: 18px; color: #64748b"></i>

            <span class="badge">3</span>

            <div class="notification-dropdown" id="notificationDropdown">
              <div class="notif-header">
                <span>Notifications</span>
                <span class="mark-read">Mark all read</span>
              </div>
              <div class="notif-list">
                <div class="notif-item">
                  <img
                    src="https://i.pravatar.cc/150?u=1"
                    class="notif-img"
                    alt="User"
                  />
                  <div class="notif-content">
                    <div class="notif-text">
                      <strong>John Doe</strong> finished the scanning
                    </div>
                    <div class="notif-time">2 mins ago</div>
                  </div>
                </div>

                <div class="notif-item">
                  <img
                    src="https://i.pravatar.cc/150?u=5"
                    class="notif-img"
                    alt="User"
                  />
                  <div class="notif-content">
                    <div class="notif-text">
                      <strong>Sarah Lee</strong> finished the scanning
                    </div>
                    <div class="notif-time">15 mins ago</div>
                  </div>
                </div>

                <div class="notif-item">
                  <img
                    src="https://i.pravatar.cc/150?u=8"
                    class="notif-img"
                    alt="User"
                  />
                  <div class="notif-content">
                    <div class="notif-text">
                      <strong>Mike K.</strong> requested admin access
                    </div>
                    <div class="notif-time">1 hour ago</div>
                  </div>
                </div>
              </div>
            </div>
          </div>
          <div class="icon-wrapper">
            <i class="fas fa-cog" style="font-size: 18px; color: #64748b"></i>
          </div>
          <div
            style="
              width: 35px;
              height: 35px;
              background: #6262a6;
              border-radius: 50%;
              color: white;
              display: flex;
              align-items: center;
              justify-content: center;
              font-weight: bold;
              cursor: pointer;
            "
          >
            AD
          </div>
        </div>
      </header>

      <div class="content">
        <h1 class="page-title">Overview</h1>

        <div class="stats-grid">
          <div class="stat-card">
            <div class="stat-header">
              <span class="stat-title">Total Scans</span>
              <div class="stat-icon bg-purple">
                <i class="fas fa-expand"></i>
              </div>
            </div>
            <div class="stat-value">128,430</div>
            <div class="stat-diff text-up">
              <i class="fas fa-arrow-up"></i> 12% vs last month
            </div>
          </div>

          <div class="stat-card">
            <div class="stat-header">
              <span class="stat-title">Activated Cards</span>
              <div class="stat-icon bg-green">
                <i class="fas fa-id-card"></i>
              </div>
            </div>
            <div class="stat-value">45,200</div>
            <div class="stat-diff text-up">
              <i class="fas fa-arrow-up"></i> 8% vs last month
            </div>
          </div>

          <div class="stat-card">
            <div class="stat-header">
              <span class="stat-title">Activating Failed Scans</span>
              <div class="stat-icon bg-red">
                <i class="fas fa-times-circle"></i>
              </div>
            </div>
            <div class="stat-value">1,204</div>
            <div class="stat-diff text-down">
              <i class="fas fa-arrow-down"></i> 2% vs last month
            </div>
          </div>

          <div class="stat-card">
            <div class="stat-header">
              <span class="stat-title">Devices Online</span>
              <div class="stat-icon bg-orange"><i class="fas fa-wifi"></i></div>
            </div>
            <div class="stat-value">84</div>
            <div class="stat-diff text-up">
              <i class="fas fa-check"></i> All Systems Active
            </div>
          </div>
        </div>

        <div class="analytics-grid">
          <div class="panel">
            <div class="panel-title">Scan Volume (7 Days)</div>
            <div class="chart-container">
              <canvas id="scanChart"></canvas>
            </div>
          </div>

          <div class="panel">
            <div class="panel-title">Top Scanners (Today)</div>
            <ul class="user-list">
              <li class="user-item">
                <div class="avatar">JD</div>
                <div class="user-info">
                  <h4>John Doe</h4>
                  <p>Gate A - Entrance</p>
                </div>
                <span class="scan-count">432</span>
              </li>
              <li class="user-item">
                <div class="avatar">AS</div>
                <div class="user-info">
                  <h4>Anna Smith</h4>
                  <p>Gate B - VIP</p>
                </div>
                <span class="scan-count">390</span>
              </li>
              <li class="user-item">
                <div class="avatar">MK</div>
                <div class="user-info">
                  <h4>Mike K.</h4>
                  <p>Warehouse Rear</p>
                </div>
                <span class="scan-count">210</span>
              </li>
              <li class="user-item">
                <div class="avatar">SL</div>
                <div class="user-info">
                  <h4>Sarah Lee</h4>
                  <p>Lobby Front</p>
                </div>
                <span class="scan-count">185</span>
              </li>
              <li class="user-item">
                <div class="avatar">DT</div>
                <div class="user-info">
                  <h4>David T.</h4>
                  <p>Parking Level 2</p>
                </div>
                <span class="scan-count">140</span>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </div>

    <script>
      const ctx = document.getElementById("scanChart").getContext("2d");

      // Gradient fill for the chart
      let gradient = ctx.createLinearGradient(0, 0, 0, 400);
      gradient.addColorStop(0, "rgba(98, 98, 166, 0.3)"); // Primary color fade
      gradient.addColorStop(1, "rgba(98, 98, 166, 0.0)");

      const myChart = new Chart(ctx, {
        type: "line",
        data: {
          labels: ["Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"],
          datasets: [
            {
              label: "Cards Scanned",
              data: [1200, 1900, 1500, 2200, 1800, 900, 1100],
              backgroundColor: gradient,
              borderColor: "#6262A6", // Primary Color
              borderWidth: 2,
              pointBackgroundColor: "#ffffff",
              pointBorderColor: "#6262A6",
              pointRadius: 5,
              pointHoverRadius: 7,
              fill: true,
              tension: 0.4, // Smooth curves
            },
          ],
        },
        options: {
          responsive: true,
          maintainAspectRatio: false, // CRITICAL: This allows the chart to fit the container height
          plugins: {
            legend: {
              display: false,
            },
            tooltip: {
              backgroundColor: "#1e293b",
              padding: 12,
              cornerRadius: 8,
            },
          },
          scales: {
            x: {
              grid: {
                display: false,
                drawBorder: false,
              },
              ticks: {
                color: "#64748b",
              },
            },
            y: {
              beginAtZero: true,
              grid: {
                borderDash: [5, 5],
                color: "#e2e8f0",
              },
              ticks: {
                color: "#64748b",
              },
            },
          },
        },
      });

      // --- NOTIFICATION DROPDOWN LOGIC ---
      const notifBtn = document.getElementById("notificationBtn");
      const notifDropdown = document.getElementById("notificationDropdown");

      // Toggle dropdown on click
      notifBtn.addEventListener("click", function (e) {
        // Prevent the click from triggering the document listener immediately
        e.stopPropagation();
        notifDropdown.classList.toggle("active");
      });

      // Close dropdown when clicking anywhere else on the page
      document.addEventListener("click", function (e) {
        if (!notifDropdown.contains(e.target) && !notifBtn.contains(e.target)) {
          notifDropdown.classList.remove("active");
        }
      });
    </script>
  </body>
</html>
